{% extends 'base.html.twig' %}

{% block title %}Registro!{% endblock %}

{% block header %}
    <div>
        <a href="{{ path('registro_student') }}" class="btn btn-blue btn-block btn-marketing rounded-pill col-md-12" style="color: white">{{ 'Registrar Alumno' | trans }}</a>
    </div>
    <br><br><br>
    <div >
        <a href="{{ path('registro_teacher') }}" class="btn btn-blue btn-block btn-marketing rounded-pill col-md-12" style="color: white">{{ 'Registrar Maestro' | trans }}</a>
    </div>
{% endblock %}
